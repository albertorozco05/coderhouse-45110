### Manejo de archivos

Go to the root of the project and run the following command:

```bash
node index.js
```

Check the output in the console and you can see the file db.json created in the next path:

```bash
/database/db.json
```